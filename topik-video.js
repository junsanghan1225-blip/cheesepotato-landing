/* TOPIK 읽기 문항의 "문제 풀이 영상".

   영상은 assets/video/topik-reading/ 에 문항 id 그대로 파일 이름을
   붙여 mp4 로 올린다(예: tr-001.mp4, t2-005.mp4).

   문항 자료(topik.js 의 손으로 쓴 배열, topik2.js 는 docs/topik2-all50.json
   에서 자동 생성되는 파일이라 손대면 안 된다)를 건드리지 않고, 영상이
   있는 id만 여기 한 줄씩 추가한다 — 영상은 문항 몇백 개 중 일부만,
   그것도 하나씩 천천히 늘어날 것이기 때문이다.

   화면(tqPick)은 이 목록에 문항 id가 있을 때만 "문제 풀이 영상" 재생기를
   보여 준다. 없으면 조용히 안 보여 준다 — 없는 영상을 가리키다 깨진
   재생기를 보여 주지 않는다. */
export const TQ_VIDEO_IDS = new Set([
  // 'tr-001',
  // 't2-005',
]);
